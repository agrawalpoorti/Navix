<% layout("/layouts/boilerplate.ejs") %>

<section class="noroute-section">
    <div class="noroute-card">
        <div class="noroute-icon">🗺️</div>
        <h1>No Route Found</h1>
        <p>There is no available path between the selected cities.</p>
        <button class="submit-btn" onclick="window.location.href='/plan'">Try Again</button>
    </div>
</section>