{
  "scripts": {
    "postinstall": "npm install --prefix Navix && npm --prefix Navix run build:engine",
    "start": "npm --prefix Navix start",
    "dev": "npm --prefix Navix run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
